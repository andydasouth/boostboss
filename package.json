{
  "name": "boostboss",
  "version": "1.0.0",
  "description": "Boost Boss — The MCP-powered ad network for AI applications",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node api/auth.test.js && node api/rtb.test.js && node api/billing.test.js && node api/campaigns.test.js && node api/track.test.js && node api/mcp.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "stripe": "^17.0.0"
  }
}
